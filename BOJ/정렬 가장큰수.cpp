#include <iostream>
#include <string>
#include <vector>

using namespace std;

string solution(vector<int> numbers) {
	string answer = "";	
	int size = numbers.size();
	
	
	
	return answer;
}

int main() {
	vector<int> numbers;
	vector<int> numbers1;
	numbers = { 6, 10 ,2 };
	numbers1 = { 3, 30, 34, 5, 9 };

	cout << solution(numbers) << '\n';
	return 0;
}