#include <iostream>
#include <string>
#include <vector>

using namespace std;

int N, number;

int solution(int N, int number) {
	int answer = 0;


	return answer;
}

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(0);

	cin >> N >> number;
	solution(N, number);

	return 0;
}